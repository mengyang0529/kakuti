// Main export file for PDFViewer component
export { default } from './PDFViewer'

// Export remaining components if needed
export { default as SearchPanel } from './components/SearchPanel'

// Export custom hooks if needed
export { default as useTextSelection } from './hooks/useTextSelection'
export { default as useScreenshot } from './hooks/useScreenshot'
